export type Icon = {
  name: 'search' | 'close';
  size?: number | string;
  color?: string;
  title?: string;
};

export type ImportedIcon = {
  size?: number | string;
  color?: string;
  title?: string;
};
