import React from 'react';
import { BiSearchAlt2 } from 'react-icons/bi';

// import * as T from './Icon.types';

const icons = {
  search: (size?: string | number, color?: string) => <BiSearchAlt2 role={'img'} color={color} size={size} title={'search icon'} />
};

export default icons;